-cloned the ardupilot repo
-installed some required packages
-enter this in the wsl terminal (for sitl server): 
./build/sitl/bin/arducopter -I0 --model quad --speedup 1 --defaults Tools/autotest/default_params/copter.parm

-enter this on the other wsl terminal (same location with the sitl server):
mavproxy.py --master tcp:127.0.0.1:5760 --map --console

alternative (using wsl terminal out of vscode, webots and QGroundController): 

transitioned to ubuntu

download webots
download necessary files
open terminal wsl:
